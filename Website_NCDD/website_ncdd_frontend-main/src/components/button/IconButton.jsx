import React from "react";

const IconButton = ({ Icon, size, className }) => {
  return (
    <div className={className}>
      <i className={`fas fa-home`}></i>
    </div>
  );
};

export default IconButton;
